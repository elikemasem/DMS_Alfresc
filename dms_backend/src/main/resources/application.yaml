spring:
  application:
    name: DMS_backend

  datasource:
    url: jdbc:postgresql://192.168.250.209:5432/dms_db
    username: ticketing
    password: ticketing

  data:
    jdbc:
      dialect: postgresql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
  servlet:
    context-path: "/api"


